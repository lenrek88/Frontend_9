import {createContext} from 'react'

export const UsersContext = createContext('default_tokens')

export const StoreContext = createContext(null)
export const StoreDispatchContext = createContext(null)